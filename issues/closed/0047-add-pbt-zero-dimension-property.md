# PBT にゼロ寸法拒否プロパティを追加し古いコメントを削除する

- Priority: Medium
- Created: 2026-06-06
- Model: DeepSeek V4 Pro
- Branch: feature/add-pbt-zero-dimension-property
- Polished: 2026-06-06
- Completed: 2026-06-07

## 目的

1. `pbt/tests/prop_lib.rs` にゼロ寸法拒否の PBT プロパティを追加する
2. 完了済みの issue 0017 を「未適用」とする古いコメントを削除する

## 優先度根拠

- ゼロ寸法拒否は単体テストでのみカバーされており、PBT プロパティが未実装。PBT でカバーすべき境界値。
- コメント「0017（ゼロ拒否）未適用のためゼロ寸法は本プロパティの範囲外」は 0017 完了後も残っており、後続の開発者を誤解させる。

## 現状

`pbt/tests/prop_lib.rs:29-30`:
```rust
/// 注: 0017（ゼロ拒否）未適用のためゼロ寸法は本プロパティの範囲外。
/// 0017 完了後にゼロ拒否プロパティを追加する。
```

`src/lib.rs:360-362` でゼロ寸法拒否は実装済み。

## 設計方針

1. `valid_i420()` ストラテジーと同様のパターンでゼロ寸法拒否プロパティを追加する
2. 古いコメントを削除または更新する

## 完了条件

- PBT に「width=0 または height=0 の場合、プレーン長に関係なく from_i420 がエラーを返す」プロパティが追加されていること
- 古い issue 番号参照を含むコメントが削除されていること
- `cargo test -p pbt --features source-build` が通過すること
