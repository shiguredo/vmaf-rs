[package]
name = "pbt"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
shiguredo_vmaf = { path = ".." }

[dev-dependencies]
# PBT (Property-Based Testing)
proptest = "1.6"

[features]
source-build = ["shiguredo_vmaf/source-build"]
